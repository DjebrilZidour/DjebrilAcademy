{/* <CourseDetail
key={idx}
title={singleCourse.courseTitle}
course={singleCourse.courseDetail}
courseImage={singleCourse.courseImage}
courseLink={singleCourse.courseLink}
/>

// Rendering the CourseDetail component with props for each course
<CourseDetail
  key={idx} // Unique key for each element in a list, required for React to identify which items have changed
  title={singleCourse.courseTitle} // Passing the course title as a prop
  course={singleCourse.courseDetail} // Passing the course detail as a prop
  courseImage={singleCourse.courseImage} // Passing the course image URL as a prop
  courseLink={singleCourse.courseLink} // Passing the course link as a prop
/> */}
